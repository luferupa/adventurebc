<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>AdventureBC</title>
  </head>
  <body>
    <div id="auth">
      <header class="header">
        <!-- anything shell header related goes here. e.g. top menu -->
        <div class="toolbar">
          <a href="#home">Home</a>
          <a href="#guide">Guide</a>
          <a href="#login">Login</a>
          <a href="#register">Register</a>
          <a id="signOut">Sign Out</a>

        </div>
      </header>
      <div id="authArea">
        <!-- the main part (auth) will be dynamically updated as each page loads -->
      </div>
    </div>
    <div id="noAuthArea">
      <!-- the no-auth part will be dynamically updated as each page loads -->
    </div>
  </body>
</html>
